# recipe-app コードレビュー (2026-09-02)

対象: `index.html` (1828行), `recipe_server.py` (484行), 設定一式
優先度: 🔴=すぐ直す / 🟠=次に直す / 🟡=改善提案

> **2026-09-02 対応済み**（セキュリティ項目は個人利用のため意図的に見送り）
> - 再描画で状態が飛ぶ問題（タイマー / チャットログ / 追加食材の選択 / アコーディオン開閉 / 入力途中のテキスト）
> - タイマーを終了時刻ベースに変更（バックグラウンドでもズレない）
> - 「これに決めた」で入力条件を消さない（softReset を新設）
> - チャット反映後も会話履歴を保持し、未反映分だけをAIに送る
> - 更新APIに全手順を渡す（先頭4ステップのみだった問題）
> - 履歴: `_keptAtISO` / `_context` / UUID の追加、保存失敗の通知、削除確認
> - **初期化順のバグを修正**（`restoreState()` が変数初期化前に走り、前回状態の復元が常に失敗していた）
> ヘッドレスブラウザで23項目の回帰テスト済み。

---

## 🔴 1. セキュリティ・即時対応

### 1-1. APIキーが平文で配置されている
`recipe_config.json` に Anthropic のキーと Spoonacular のキーが生値で入っている。
- `.gitignore` 済み・git履歴にも無いことは確認済み（漏洩はしていない）
- ただしローカルファイルとして残り続けるのは危険。**キーはローテーションし、`.env` + 環境変数のみに移行**するのが安全
- `spoonacular_api_key` / `max_recipes_from_spoonacular` はもう未使用 → 削除

### 1-2. Flask の static_folder 設定でソースが公開されうる
```python
app = Flask(__name__, static_folder=BASE_DIR)
```
`static_url_path` を指定していないため、Flask は `/<フォルダ名>/<ファイル名>` を自動で静的配信する。
つまり `/recipe-app/recipe_server.py` や `/recipe-app/recipe_config.json` でソースと設定が読める。
Render 上は config が deploy されないので実害は小さいが、**ローカルを `0.0.0.0` で起動している以上、同一LANから見える**。
→ `static_folder=None` にして `send_from_directory` のみで配信する。

### 1-3. API が完全に無認証・無制限
`/api/recipe` `/api/chat` を誰でも叩ける = **他人が SHO さんの Anthropic 課金を消費できる**。
keepalive workflow で常時起動しているので露出も継続的。
- 最低限: 簡易トークン（`X-App-Token`）か Origin チェック
- IPごとのレート制限（1分5回など）
- `ingredients` の件数・文字数上限、`history` の長さ上限

### 1-4. 内部例外がそのままクライアントに返る
```python
except Exception as e:
    return jsonify({"error": str(e)}), 500
```
スタック由来の内部情報が漏れる。ログには出し、レスポンスは固定文言に。

### 1-5. 食材タグが innerHTML にエスケープなしで入る（自己XSS）
`renderTags()` / `restoreState()`:
```js
el.innerHTML = t + '<span class="tag-del" ...>';   // t は未エスケープ
```
食材名に `<img src=x onerror=...>` を入れると実行され、**localStorage に永続化される**。
→ `esc(t)` を通す。また `esc()` は `'` を変換していないので `&#39;` も追加する。

---

## 🟠 2. 過去レシピ（決定した献立）の表示

現状: 横並びタブ → タップでモーダル1件表示（静的カード）。

| 問題 | 内容 |
|---|---|
| 一覧性 | 件数が増えると横スクロールのタブが延々並ぶだけ。検索・絞り込み・並び替えが一切ない |
| メタ情報が死んでいる | `_keptAt` を `toLocaleString('ja-JP')` の**文字列でしか保存していない** → 日付ソート・期間フィルタが不可能。ISO文字列も併存させる |
| 1件ずつしか見られない | モーダルなので比較不能。カードのグリッド表示や「2件を並べる」が欲しい |
| 静的すぎる | `renderStaticRecipeCard` にはコピー・タイマー・チャット・更新が無い（→ 4章） |
| 保存の失敗が無言 | `saveHistoryList` の `catch(e) {}` が握り潰し。localStorage 5MB 上限に達すると**保存されないまま気づけない**。件数上限（例50件）+ 失敗トーストを |
| 削除が危険 | ✕タップで即削除。確認もundoも無い |
| ID衝突 | `_id = Date.now() + '-' + idx` は同一ms・同一idxで衝突しうる。`crypto.randomUUID()` に |
| 出口が無い | 保存レシピの共有・エクスポート・買い物リスト化が無い |

**設計提案**: 履歴を「献立ノート」に格上げする。
- 保存時に `ingredients`（元の食材）、`mood`、`servings` も一緒に保存 → 「同じ食材で何を作ったか」が辿れる
- 一覧はカードグリッド + 食材タグでのフィルタ + 日付ソート
- 「この献立をもう一度」→ 入力欄に食材を復元して再提案

---

## 🟠 3. 複数レシピの同時閲覧

現状: 上部にサマリー行（横スクロール）、下に詳細カードを縦積み。

| 問題 | 内容 |
|---|---|
| 比較ができていない | `sum-card` は要約のみ。材料が見えないので「どっちが手持ちで作れるか」が比べられない。**サマリーに主要食材3つと不足食材を出す**だけで比較機能として成立する |
| 献立としての合成が無い | 3品から複数選んで「**合算の買い物リスト**」「**並行調理のタイムライン**（工程をマージして時系列に並べる）」を出す機能が無い。ここがこのアプリの一番の伸びしろ |
| 決定は1品ずつ | `keepDish(idx)` は1件のみ。チェックボックスで複数選択 → まとめて決定 が自然 |
| 🔴 決定すると全部消える | `keepDish` → `resetAll()` で**入力食材まで全消去**。「同じ食材で副菜をもう一品」という一番ありそうな流れで食材を打ち直させている。食材・人数・気分は残すべき |
| 🔴 タイマーが再描画で全消滅 | `renderResults` 冒頭で `clearInterval` するため、代替食材の選択やレシピ更新をすると**調理中のタイマーが全部止まる**。事故につながる |
| タイマーがずれる | 残り秒数を `setInterval` のカウントで持っているため、スマホでバックグラウンドに回ると遅延・停止する。**終了時刻(epoch)を持って差分計算**に変える。終了通知も鳴らない（Notification API か画面復帰時の補正が必要） |
| スクロール追従 | IntersectionObserver は入っているが、詳細カードが長いのでサマリー行への「戻る」導線（追従ヘッダ）があると良い |

---

## 🔴 4. 保存後のAI相談・更新（ここが一番弱い）

**保存した瞬間にレシピが読み取り専用になる。** これが構造的な欠陥。

1. **保存レシピにチャットが無い**
   `openKeptModal` → `renderStaticRecipeCard` はチャットUIも更新ボタンも生成しない。
   実際に作るのは「決定した後」なので、**相談が必要になるのは保存後**。順序が逆になっている。

2. **チャットログが再描画で消える**
   `chatHistories[idx]` はJSに残るが、`renderResults` が `chat-msgs-N` を作り直すときに**過去メッセージを再描画していない**。
   代替食材を1つ選ぶ（`selectSub`）だけで会話が画面から消える。→ 再描画時に `chatHistories` からDOMを復元する処理が必要。

3. **更新すると会話履歴が消去される**
   ```js
   chatHistories[idx] = [];   // applyRewriteFromChat
   ```
   「更新 → さらに相談 → 再更新」ができない。文脈が切れるので継続的な作り込みが不可能。
   履歴は残し、`applied_at` のマーカーを入れて「ここまで反映済み」と表示するのが正しい。

4. **チャット・選択状態が永続化されない**
   `saveState()` は `recipes` と入力条件しか保存しない。`chatHistories` / `selectedSubs` / `selectedAdditions` はリロードで消える。

5. **追加食材の選択が見た目だけリセットされる**
   `toggleAddition` は `btnEl.classList.add('chip-add--selected')` とDOM直操作。`renderResults` で再構築される際に復元されないため、**データ上は選択済みなのに見た目は未選択**という不整合が起きる。

6. **更新結果が上書きで、履歴が残らない**
   `applyRewrite` / `applyRewriteFromChat` は `ingredients_ja` / `steps_ja` / `tips` を直接上書き。元に戻せない。バージョン履歴 or undo が欲しい。

7. **更新APIが元レシピの一部しか見ていない**
   ```python
   - 元の手順（概要）: {' / '.join((recipe.get('steps_ja') or [])[:4])}
   ```
   **先頭4ステップしかAIに渡していない**ので、後半の手順が勝手に作り直される。全ステップを渡すべき。
   さらに `pairing` / `cook_time_min` / `difficulty` は更新対象外なので、食材を大きく変えてもペアリングと時間が古いまま残る。

8. **エラー表示が不統一**
   `applyRewrite` は `alert()`、`applyRewriteFromChat` は `showToast()`、他は `showError()`。3系統が混在。

---

## 🟠 5. サーバー側

- **デッドコード**: `stream_recipe_response()` は `import time` が無く、呼ばれれば即 `NameError`。しかもどのルートからも呼ばれていない。フロント側の `parseSse` / `readRecipeStream`（約60行）も対応するSSEルートが存在せず未使用。両側で約150行の死んだコード
- **`recipe_server_fixed.py` の残置**: 本番は `recipe_server.py`（render.yaml）。差分は1行（`exclude_titles` を渡すか否か）だけ。混乱の元なので削除
- **`_jobs` のメモリリーク**: 完了ジョブが永久に残る。TTL（例30分）で掃除する
- **`/api/recipe` と `/api/recipe/next` が完全に同一コード**。統合できる
- **スケール不可**: ジョブがプロセス内メモリなので `--workers 1` 固定。worker を増やした瞬間に「job not found」で壊れる。増やすなら Redis 等が必要（当面は workers 1 のままで良いが、コメントで明示を）
- **ポーリングにタイムアウトが無い**: `pollJob` は `pending` の間 2秒ごとに無限リトライ。ジョブが死ぬと永久ループ。最大リトライ回数を設ける
- **入力バリデーション不足**: `int(body.get("servings", 2))` は不正値で `ValueError` → 500
- **AI応答のスキーマ検証が無い**: `title_ja` 欠落や配列でないJSONで画面が壊れる。最低限のバリデーションを
- **チャットのコンテキストが毎回肥大**: system に全手順を毎回埋め込み + history 無制限 → 会話が伸びるほど課金が増える。直近N往復に制限を

---

## 🟡 6. 保守性・その他

- **1828行の単一HTML**。CSS/JS/テンプレートが全部混在、文字列連結でHTML生成、`onclick` 属性でハンドラ結線、グローバル変数が15個以上。
  全面書き換えは不要だが、**最低限 `app.js` / `style.css` に分離**するだけで見通しが大きく改善する
- **再描画がすべて全体再構築**。`el.innerHTML = ...` で毎回作り直すため、`<details>` の開閉状態・チャットのスクロール位置・入力途中のテキストがすべて失われる。差分更新（該当カードのみ再描画）にするだけで体感が変わる
- **テストが0件**。特に `call_claude` のJSON抽出フォールバックと `parseStepTime` は壊れやすく、テストの費用対効果が高い
- **`parseStepTime` の誤検出**: 「3〜4人分」など時間以外の数値を拾う可能性。「〜分」の前後文脈チェックを
- **代替食材のキー突合が部分一致**: `ing.indexOf(k) !== -1` なので「肉」が「鶏もも肉 300g」に誤マッチしうる
- **タグ入力の blur 自動確定**: ✕（削除）をタップした際に blur が先に走り、意図しないタグ追加が起きうる
- **アクセシビリティ**: モーダルに Esc キー・フォーカストラップ・`aria-*` が無い
- **PWA化されていない**: 台所でスマホで使う前提なら、ホーム画面追加・オフラインで保存レシピ閲覧・画面スリープ抑止（Wake Lock）は効果が大きい
- **料理画像が無い**: 選択の判断材料として弱い
- **モデルが haiku 固定**: 提案の質を上げたい場面で切り替えられない

---

## 推奨する着手順

1. 🔴 APIキーのローテーション + `static_folder` 修正 + タグの `esc()` （30分）
2. 🔴 チャットログの再描画復元 + 更新後も履歴を残す（保存後の相談が成立する）
3. 🔴 `keepDish` で食材を消さない + タイマーを再描画で殺さない
4. 🟠 保存レシピにチャット/更新/コピーを付ける（静的カードを通常カードに統合）
5. 🟠 API のレート制限 + 入力バリデーション
6. 🟠 デッドコード削除（SSE一式、`recipe_server_fixed.py`）
7. 🟡 複数選択 → 買い物リスト / 並行調理タイムライン（新機能・伸びしろ）
